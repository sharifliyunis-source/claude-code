import type { Metadata } from "next";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: {
    default: "Middle Corridor News",
    template: "%s | Middle Corridor News",
  },
  description:
    "Latest news on the Trans-Caspian International Transport Route (TITR) — the Middle Corridor connecting Asia and Europe.",
  openGraph: {
    siteName: "Middle Corridor News",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="h-full">
      <body className="min-h-full flex flex-col antialiased">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
